سوال یازدهم:

برنامه بنویسید که یک رشته کد به زبان پایتون را اجرا کند.

به عنوان نمونه رشته زیر به شما داده شده است:
code = "print('Hello')"

کد نوشته شده داخل این رشته را اجرا کنید. ( نیازمند سرچ در اینترنت)